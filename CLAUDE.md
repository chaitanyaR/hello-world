# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Overview

This is a bare starter repository ("hello-world") with no source code, build system, or test framework established yet. The `.gitignore` is pre-configured for C/C++ compiled artifacts (object files, shared libraries, executables), suggesting the intended language, but no source files exist as of the initial commits.

## Current Contents

- `README.md` — minimal project description
- `test` — placeholder file (contains "test")
- `test branch` — placeholder file added via a test branch/PR
- `.gitignore` — covers C/C++ build outputs (`.o`, `.a`, `.so`, `.dll`, `.exe`, `.out`, etc.)

## Branching Convention

Based on the existing PR history, feature work is done on named branches and merged into `master` via pull request.
